import { quartet1List } from "./sublists/quartet1List";
import type { MainList } from "@/types/types";

export const quartetList: MainList = {
  name: "Quartet",
  value: "quartet",
  sublists: [quartet1List]
};
